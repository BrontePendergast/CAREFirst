---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: lab4
spec:
  hosts:
    - jhsmith.mids255.com
  gateways:
    - istio-ingress/jhsmith-gateway
  http:
    - match:
        - uri:
            exact: /conversations
        - uri:
            prefix: /health
        - uri:
            prefix: /hello
        - uri:
            prefix: /docs
        - uri:
            prefix: /openapi.json
      route:
        - destination:
            host: lab4
            port:
              number: 8000
